import config from '../config.js';

export const creatorInfo = {
  name: config.ownerName || "Davex-254",
  github: "https://github.com/VENOM-X",
  contact: `https://wa.me/${config.ownerNumber.replace(/\+/g, '')}`
};

export default creatorInfo;